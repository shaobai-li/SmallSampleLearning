data:
  metadata_path: data/original/val.csv       # 原始元数据 CSV
  features_path: outputs/features/val_features.csv  # AE3D 特征 CSV

output:
  path: outputs/val_merged.csv

merge:
  old_prefix: "feature_"        # 旧特征前缀

