data:
  metadata_path: data/original/train.csv       # 原始元数据 CSV
  features_path: 
    - outputs/features/train_features.csv  # AE3D 特征 CSV
    - outputs/features/train_features_2p5d.csv  # AE2p5D 特征 CSV

output:
  path: outputs/train_merged.csv

merge:
  old_prefix: 
    - "feature_"
    - "f_"