data:
  csv_path: data/train_ssl.csv
  nrrd_dir: /data/res/res3d/inbox/02_masked/  # 修改为你的 NRRD 文件目录
  bin_factor: 2

model:
  name: AE_2p5D  # 2.5D 自编码器（切片插值）

training:
  batch_size: 4
  epochs: 50
  lr: 1e-4
  num_workers: 4

checkpoint:
  save_interval: 10
  dir: checkpoints
